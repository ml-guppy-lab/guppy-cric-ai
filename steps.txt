git credential reject
protocol=https
host=github.com

initialize repository: git remote add origin https://github.com/ml-guppy-lab/fire_writing.git

cd /path/to/your/new/repo
git config user.email "sonalsh250@gmail.com"

python3 -m venv .venv
source .venv/bin/activate

.venv/bin/python3 -m pip install --upgrade pip
.venv/bin/python3 -m pip install -r requirements.txt

after setting up basic backend and frontend:
- from outside backend folder: .venv/bin/uvicorn backend.main:app --reload
- from outside frontend folder: cd frontend && npm run dev

